# CLAUDE.md — プロジェクト・ブリーフ

> ターミナルの Claude Code が最初に読むファイルです。詳細な経緯は
> [`docs/引き継ぎ.md`](docs/引き継ぎ.md) を参照してください。

## このプロジェクトは何か

リフォーム現地調査の**ヒアリング／調査の抜け漏れをゼロにする**支援システム。
新人がタブレットで入力すると、必須チェック・写真もれ防止・アップセル確認が自動で働く仕組みを目指す。

（リポジトリ名は `space-invaders-modern` だが中身はこのシステム。元は React+Vite の雛形を転用したもの。）

## 絶対に満たすべき2大前提

1. **新入社員でも、現地調査で漏れなく情報を収集できる**こと
2. **お客様のご要望がトイレ工事だけでも、アップセル（追加提案）の確認ができる**こと

## 現在の到達点（2026-08 時点）

- **要件定義 v4** … `docs/genchi_hearing_tatakidai_v4.md`（御社の現行チェックシート9枚の実項目を反映済み）
- **入力UIアプリ**（React + Vite）… `src/` 一式。要件定義v4を実際に動くタブレット入力UIとして実装済み。
- **実機プレビュー** … Artifact で公開（詳細は引き継ぎドキュメント）。GitHub Pages 用ワークフローも用意（Pages の手動有効化待ち）。
- 作業ブランチ: `claude/requirements-definition-dev-hm7c3n`

## 開発コマンド

```bash
npm install
npm run dev      # 開発サーバ
npm run build    # 本番ビルド（GitHub Pages 用に base=/space-invaders-modern/）
npm run lint     # ESLint（全角スペースは文字列/コメント/JSX/テンプレートで許可）
```

## コード構成

```
src/
  data/surveyModel.js   … 要件定義v4準拠の項目・新人ガイド・寿命めやす・条件分岐の「単一の情報源」
  lib/helpers.js        … 空判定・必須判定・アップセルの芽判定
  components/           … Header / Field / GuidePanel / PartScan / Summary
  App.jsx               … ガイド型ウィザード制御・完了ゲート・サマリー計算・localStorage保存
docs/
  genchi_hearing_tatakidai_v4.md … 要件定義 v4
  引き継ぎ.md                      … 経緯・成果物リンク・残論点・次アクション（まず読む）
.github/workflows/deploy-pages.yml … GitHub Pages 自動デプロイ
```

> 項目の追加・変更は基本 `src/data/surveyModel.js` を編集すればUI全体（ウィザード・必須判定・サマリー）に反映される設計。

## 実装済みの主要ルール（要件定義 第6部）

- 全部位スキャンの**完了ゲート**（8部位＝水回り5＋内装3の対象判定＋アップセル診断3点が埋まるまで完了不可）
- 必須未入力のブロック／写真もれ防止リスト
- 条件分岐（マンション→管理規約等／築1981以前→耐震項目）
- 寿命めやす超過・劣化「要注意」で**アップセルの芽**を自動抽出
- 完了時に台帳転記データ（JSON）をプレビュー（実台帳連携は次段階）

## 作業ルール（厳守・元引き継ぎメモより）

- **個人情報（邸名・氏名・電話・メール・住所）は成果物に含めない**。元シートに含まれる場合は除外。
- Google Drive へ書き込む前・ファイルにアクセスする前は、保存先・操作範囲を確認してから実行する。
- 認証情報（APIキー・パスワード等）は扱わない。必要時は環境変数（.env）参照の形で。
- 作業は小さく区切り、節目で確認を取る。

## 確定している設計判断

- 部位構成は現行シートに合わせて確定：**水回り5**（UB／在来浴室／キッチン／洗面所／トイレ）＋**内装3**（クロス／和室→洋室／間仕切り壁）＝8部位。
- **外装系**（外壁・屋根・防水・給湯機の単独）は現行シート未整備のため「今後追加枠」。給湯機は浴室シートの熱源欄で一部カバー。
- 内装系はアップセル診断3点の「型番」を「現況仕様（材質・写真）」に読み替え。

## 次の候補ステップ（未着手・要相談）

1. GitHub Pages の有効化（管理者が Settings→Pages→Source=GitHub Actions を1回設定）→ 常設プレビューURL公開
2. UI の項目・文言の調整（現場目線でのレビュー反映）
3. スプレッドシート台帳への自動転記の実装
4. システム要件定義書の作成
5. 新人ガイドの見本写真・スクリプト本文・寿命めやす数値の拡充（現状は一般値／プレースホルダ）
